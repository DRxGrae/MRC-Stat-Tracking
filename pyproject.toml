[project]
name = "mrc-discord-bot"
version = "0.1.0"
description = "Discord bot for MRC stat tracking"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "discord.py>=2.4.0,<3",
  "numpy>=1.26.0",
  "pillow>=10.0.0",
]

[dependency-groups]
dev = []
